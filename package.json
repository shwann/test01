{
  "name": "test01-company-portal",
  "version": "1.0.0",
  "private": true,
  "description": "A static company portal homepage.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check main.js && node server.js --check"
  }
}
